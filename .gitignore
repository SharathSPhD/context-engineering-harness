.worktrees/
__pycache__/
*.pyc
.venv/
.coverage
htmlcov/
mlruns/

# Secrets: never commit tokens (HF_TOKEN, ANTHROPIC_API_KEY, etc.)
.env
.env.*
!.env.example

# Scheduler local cache + cost ledger (per-machine, large, non-portable)
.cache/

# Ralph loop / power-ext run logs (per-machine, regenerated on each run)
tools/dev/orchestration/logs/

# Private planning artifacts — user-facing docs (guide.md, validation_report.md) are tracked
docs/prd.md
docs/spec.md
docs/triz.md
docs/research.md
docs/superpowers/
.triz/
.DS_Store
